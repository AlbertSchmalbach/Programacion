function cuadrado(nums) {
    return nums ** 2;
}

// let numero = parseInt(prompt("Ingrese un numero ", 2));
let numero = 8

console.log(`el cuadrado de ${numero} es ${cuadrado(numero)}`);
// alert(`el cuadrado de ${numero} es ${cuadrado(numero)}`);